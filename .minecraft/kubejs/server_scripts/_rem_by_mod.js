// priority: 0
// Important to load this first to remove an entire mod's recipes
/* const MODS = ["toughasnails"];

ServerEvents.recipes((event) => {
	MODS.forEach((element) => {
		event.remove({
			modid: element,
		});
	});
}); */
